// Archived 2026-04-28
// Reason: hardcoded Twelve Data API key currently unused — production calls use `apikey=demo`.
// Do NOT import. Reference only — kept per project policy.
//
// Verifier note: the previously-archived _closedFx/_closedMetal/_closedUsIdx helpers
// were RESTORED to js/market-hours.js because isMarketOpen() actually calls them.
// Earlier analysis mis-flagged them as dead.

const _TD_KEY = 'a0817e6f3bd94d4b8a1a5e8c7f2d9e10';
